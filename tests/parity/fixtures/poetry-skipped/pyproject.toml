[tool.poetry]
name = "example"
version = "0.1.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.10"
requests = { git = "https://github.com/requests/requests.git" }
local-pkg = { path = "../local-pkg" }
