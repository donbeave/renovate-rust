# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/flux/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/flux/artifacts.spec.ts
**Total tests:** 7 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| replaces existing value | 15 | not-applicable | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| detects system manifests in subdirectories | 54 | not-applicable | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| ignores non-system manifests | 91 | not-applicable | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| ignores unchanged system manifests | 102 | not-applicable | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| ignores system manifests without a new version | 121 | not-applicable | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| failed to generate system manifest | 132 | not-applicable | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| failed to read system manifest | 151 | not-applicable | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests flux artifact update via vitest-mocked fs/exec; Rust tests this at different layer |

---

