# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/nix/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/nix/artifacts.spec.ts
**Total tests:** 10 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns if no flake.lock found | 55 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if unchanged | 68 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated flake.lock | 88 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| adds GitHub token | 117 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| trims "x-access-token:" prefix from GitHub token | 147 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| supports docker mode | 177 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| supports install mode | 222 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| catches errors | 256 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated flake.lock when doing lockfile maintenance | 275 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| uses nix from config | 304 | not-applicable | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nix artifact update via vitest-mocked fs/exec; Rust tests this at different layer |

---

