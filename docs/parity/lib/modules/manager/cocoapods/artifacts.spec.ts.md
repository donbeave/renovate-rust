# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/cocoapods/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/cocoapods/artifacts.spec.ts
**Total tests:** 11 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns null if no Podfile.lock found | 54 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if no updatedDeps were provided | 67 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null for invalid local directory | 80 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if updatedDeps is empty | 97 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if unchanged | 110 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated Podfile | 132 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated Podfile and Pods files | 156 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| catches write error | 189 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns pod exec error | 210 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| dynamically selects Docker image tag | 231 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| supports install mode | 274 | not-applicable | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests cocoapods artifact update via vitest-mocked fs/exec; Rust tests this at different layer |

---

