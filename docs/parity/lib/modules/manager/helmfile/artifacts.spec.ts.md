# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/helmfile/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/helmfile/artifacts.spec.ts
**Total tests:** 9 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns null if no helmfile.lock found | 83 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if updatedDeps is empty | 95 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if unchanged | 106 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated helmfile.lock | 128 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated helmfile.lock if repositories were defined in ../helmfile-defaults.yaml. | 159 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| log into private OCI registries, returns updated helmfile.lock | 219 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| docker run --rm --name=renovate_sidecar --label=renovate_child | 310 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| not found | 391 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| updates lockfile with multidoc YAML | 421 | not-applicable | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests helmfile artifact update via vitest-mocked fs/exec; Rust tests this at different layer |

---

