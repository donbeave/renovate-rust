# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/pip_requirements/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/pip_requirements/artifacts.spec.ts
**Total tests:** 8 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns null if no updatedDeps were provided | 50 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if no hashes | 61 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if unchanged | 73 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated file | 97 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| ignores falsy depNames | 129 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| catches and returns errors | 161 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| supports docker mode | 190 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |
| supports install mode | 244 | not-applicable | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests pip_requirements artifact update via vitest-mocked fs/exec; Rust tests this at different layer |

---

