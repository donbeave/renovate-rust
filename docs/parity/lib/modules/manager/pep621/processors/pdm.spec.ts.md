# Renovate Test Detail

[Back to test map](../../../../../renovate-test-map.md)

## `lib/modules/manager/pep621/processors/pdm.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/pep621/processors/pdm.spec.ts
**Total tests:** 8 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `updateArtifacts()`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| throws TEMPORARY_ERROR | 31 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |
| return null if there is no lock file | 40 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |
| return null if the lock file is unchanged | 55 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |
| returns artifact error | 111 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |
| return update dep update | 135 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |
| discard dependencies if the devGroup is missing | 230 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |
| return update on lockfileMaintenance | 273 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |
| sets Git environment variables | 318 | not-applicable | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer | — | Mock framework internals — tests pep621/pdm via vitest-mocked fs/exec/datasource; Rust tests this at different layer |

---

