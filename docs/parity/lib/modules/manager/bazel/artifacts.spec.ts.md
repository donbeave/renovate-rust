# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/bazel/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/bazel/artifacts.spec.ts
**Total tests:** 15 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| updates commit-based http archive | 9 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates http archive with content other then WORKSPACE | 69 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates finds url instead of urls | 127 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| throws error if no urls resolve hashes | 186 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| errors for http_archive without urls | 232 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| errors for maybe(http_archive) without urls | 259 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| errors for _http_archive without urls | 287 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| errors for maybe(_http_archive) without urls | 314 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates http_archive with urls array | 342 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates maybe(http_archive) with urls array | 409 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates _http_archive with urls array | 476 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates maybe(_http_archive) with urls array | 543 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates one http_archive alongside others | 610 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates one http_archive alongside others with matching versions | 680 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| migrates rules_webtesting URL format | 752 | not-applicable | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests bazel artifacts via vitest-mocked fs/exec; Rust tests this at different layer |

---

