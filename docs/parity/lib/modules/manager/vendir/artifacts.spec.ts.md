# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/vendir/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/vendir/artifacts.spec.ts
**Total tests:** 14 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns null if no vendir.lock.yml found | 45 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if empty vendir.lock.yml found | 57 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if updatedDeps is empty | 71 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if unchanged | 82 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated vendir.lock | 104 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated vendir.yml for lockfile maintenance | 133 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| catches errors | 161 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| rethrows for temporary error | 188 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| add artifacts to file list if vendir.yml exists | 214 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| add artifacts | 288 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| sets GIT_CONFIG variables when Host Rules are configured | 349 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| works explicit global binarySource | 431 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports install mode | 461 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |

### `Docker`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns updated vendir.yml for lockfile maintenance | 548 | not-applicable | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests vendir artifacts via vitest-mocked fs/exec; Rust tests this at different layer |

---

