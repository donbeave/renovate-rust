# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/nuget/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/nuget/artifacts.spec.ts
**Total tests:** 13 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| re-throws TEMPORARY_ERROR | 53 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| aborts if no lock file found | 72 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| aborts if lock file is unchanged | 87 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| runs workload restore and updates lock file | 121 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does not update lock file when non-proj file is changed | 174 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates lock file for Directory.Build.props | 194 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| updates lock file for nested Directory.Build.props | 231 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does not update lock file when no deps changed | 268 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| performs lock file maintenance | 288 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports docker mode | 331 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports install mode | 397 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports global mode | 451 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| catches errors | 492 | not-applicable | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests nuget artifacts via vitest-mocked fs/exec; Rust tests this at different layer |

---

