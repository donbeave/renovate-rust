# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/manager/composer/artifacts.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/manager/composer/artifacts.spec.ts
**Total tests:** 30 | **Ported:** 0 | **Actionable:** 0 | **Status:** done

### `tests`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns if no composer.lock found | 69 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| returns null if unchanged | 80 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| uses hostRules to set COMPOSER_AUTH | 114 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| git-tags hostRule for github.com set github-token in COMPOSER_AUTH | 191 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| Skip github application access token hostRules in COMPOSER_AUTH | 225 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| github hostRule for github.com with x-access-token set github-token in COMPOSER_AUTH | 263 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does set github COMPOSER_AUTH for github when only hostType git-tags artifactAuth does not include composer | 297 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does set github COMPOSER_AUTH for git-tags when only hostType github artifactAuth does not include composer | 336 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does not set github COMPOSER_AUTH when artifactAuth does not include composer, for both hostType github & git-tags | 375 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does not set gitlab COMPOSER_AUTH when artifactAuth does not include composer | 407 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does not set packagist COMPOSER_AUTH when artifactAuth does not include composer | 448 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does set gitlab COMPOSER_AUTH when artifactAuth does include composer | 509 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does set packagist COMPOSER_AUTH when artifactAuth does include composer | 553 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| returns updated composer.lock | 620 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports vendor directory update | 652 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| performs lockFileMaintenance | 713 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports docker mode | 749 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports install mode | 823 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| supports global mode | 881 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| catches errors | 914 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| catches unmet requirements errors | 938 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| throws for disk space | 957 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| disables ignorePlatformReqs | 976 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| adds all ignorePlatformReq items | 1011 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| installs before running the update when symfony flex is installed | 1046 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| installs before running the update when symfony flex is installed as dev | 1095 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| does not disable plugins when configured globally | 1144 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| disable plugins when configured locally | 1169 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| includes new dependency version in update command | 1194 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |
| uses --with-all-dependencies instead of --with-dependencies when composerUpdateAllDependencies is set in postUpdateOptions | 1216 | not-applicable | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer | — | Mock framework internals — tests composer artifacts via vitest-mocked fs/exec; Rust tests this at different layer |

---

