# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/datasource/go/releases-direct.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/datasource/go/releases-direct.spec.ts
**Total tests:** 15 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `modules/datasource/go/releases-direct › getReleases`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| returns null for null getDatasource result | 26 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| throws for getDatasource error | 34 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| processes real data | 43 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support forgejo | 69 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support gitlab | 130 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support gitea | 148 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support git | 209 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support self hosted gitlab private repositories | 228 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support bitbucket tags | 247 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support ghe | 269 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| works for known servers | 298 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| support gitlab subgroups | 327 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| works for nested modules on github | 347 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| falls back to unprefixed tags | 383 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |
| works for nested modules on github v2+ major upgrades | 409 | not-applicable | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer | — | Mock framework internals — tests go releases-direct via nock HTTP mocks; Rust tests this at different layer |

---
