# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/modules/datasource/docker/dockerhub-cache.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/modules/datasource/docker/dockerhub-cache.spec.ts
**Total tests:** 9 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable-applicable

### `modules/datasource/docker/dockerhub-cache`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| initializes empty cache | 74 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| initializes cache with data | 90 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| reconciles new items | 104 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| reconciles existing items | 134 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| asks for the next page if the expected count does not match cached items | 158 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| reconciles deleted items | 182 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| returns cached digest for a known tag | 200 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| returns cached arch-specific digest | 208 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |
| reconciles from empty cache | 218 | not-applicable | Mock framework internals — tests dockerhub cache via vitest-mocked package cache; Rust tests this at different layer | — | DockerHub-specific cache |

---

