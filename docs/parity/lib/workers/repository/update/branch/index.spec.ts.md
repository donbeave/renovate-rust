# Renovate Test Detail

[Back to test map](../../../../../renovate-test-map.md)

## `lib/workers/repository/update/branch/index.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/workers/repository/update/branch/index.spec.ts
**Total tests:** 101 | **Ported:** 0 | **Actionable:** 0 | **Status:** done

### `workers/repository/update/branch/index › processBranch`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| skips branch if not scheduled and branch does not exist  | 157 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch creation if minimumGroupSize is not met  | 167 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if not scheduled and not updating out of schedule  | 180 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch for fresh release with minimumReleaseAge  | 198 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if minimumReleaseAge not met  | 223 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |

### `workers/repository/update/branch/index › processBranch › if release is missing releaseTimestamp with minimumReleaseAge set`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| skips branch if minimumReleaseAgeBehaviour=timestamp-required  | 241 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does not skip branch if minimumReleaseAgeBehaviour=timestamp-optional  | 260 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does not skip branch if minimumReleaseAgeBehaviour=timestamp-required and minimumReleaseAge=0 days  | 275 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if minimumConfidence not met  | 291 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| processes branch if minimumConfidence is met  | 310 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| processes branch if not scheduled but updating out of schedule  | 329 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if closed major PR found  | 343 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if closed digest PR found  | 358 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| allows branch but disables automerge if merged PR found  | 373 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if closed minor PR found  | 388 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| allows branch even if merged PR found  | 402 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| throws error if closed PR found  | 418 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does not skip branch if edited PR found with rebaseLabel  | 432 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if edited PR found  | 451 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if tagretBranch of update PR is changed by user  | 478 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if edited PR found without commenting  | 510 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if target branch changed  | 534 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if branch edited and no PR found  | 570 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| continues branch if branch edited and but PR found  | 581 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch if branch edited and and PR found with sha mismatch  | 595 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch creation limit exceeded  | 607 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch does not exist and in silent mode  | 624 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch needs dependencyDashboardApproval  | 642 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if pr creation limit exceeded and branch exists  | 660 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if commits per run limit exceeded  | 683 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does not return if commits per run limit exceeded but rebase requested  | 707 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if commits hourly limit exceeded  | 727 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does not return if commits hourly limit exceeded but rebase requested  | 746 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if no work  | 768 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if pending checks  | 785 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if pending checks - but branch exists  | 801 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| automerges when there is no pr and, pr-creation is off-schedule  | 826 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch automerged  | 847 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch automerged and no checks  | 865 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch automerged (dry-run)  | 886 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch exists and prCreation set to approval  | 906 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch exists but pending  | 931 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch automerge is pending  | 957 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if PR creation failed  | 983 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| handles unknown PrBlockedBy  | 1009 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| retries setting branch status checks after PR creation  | 1035 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does not retry setting branch status checks when PR is not created  | 1067 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns if branch exists but updated  | 1099 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| updates branch when no fingerprint match  | 1130 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| updates branch when forceRebase=true  | 1162 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and comments notice  | 1194 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| fetches changelogs for the "branch" stage  | 1228 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and tries automerge  | 1247 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR when impossible to automerge  | 1271 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR when impossible to automerge with mismatch keepUpdatedLabel  | 1299 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips when automerge is off schedule  | 1328 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR when impossible to automerge because off schedule  | 1364 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and adds lock file error comment with default message if no releaseTimestamp  | 1392 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and adds lock file error comment with user configured message if no releaseTimestamp  | 1422 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and adds lock file error comment with templated user configured message if no releaseTimestamp  | 1458 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and adds lock file error comment if old releaseTimestamp  | 1495 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and adds lock file error comment if new releaseTimestamp and branch exists  | 1520 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| throws error if lock file errors and new releaseTimestamp  | 1545 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| ensures PR and adds lock file error comment recreate closed  | 1568 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| swallows branch errors  | 1593 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| throws and swallows branch errors  | 1606 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| rebases branch onto new basebranch if baseBranch changed by user  | 1627 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| rebases branch onto new basebranch if no fingerprint found  | 1660 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| rebases branch onto new basebranch if no fingerprint found - 2  | 1700 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| swallows pr errors  | 1735 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| closed pr (dry run)  | 1761 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| branch pr no rebase (dry run)  | 1776 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| branch pr no schedule lockfile (dry run)  | 1797 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| branch pr no schedule (dry run)  | 1842 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| branch pr no schedule  | 1890 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch update if stopUpdatingLabel presents  | 1932 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| skips branch update if same updates  | 1968 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| updates branch if stopUpdatingLabel presents and PR rebase/retry box checked  | 1996 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| updates branch if stopUpdatingLabel presents and dependency dashboard box checked  | 2038 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| executes post-upgrade tasks if trust is high  | 2077 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| handles post-upgrade task exec errors  | 2176 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| executes post-upgrade tasks with disabled post-upgrade command templating  | 2260 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| executes post-upgrade tasks with multiple dependecy in one branch  | 2354 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| executes post-upgrade tasks once when set to branch mode  | 2521 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| executes post-upgrade tasks with propagated post-upgrade file path via env variable  | 2650 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| should not propagate post-upgrade file path via env variable if the post-upgrade file creation failed  | 2755 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns when rebaseWhen=never  | 2862 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| continues when rebaseWhen=never and keepUpdatedLabel  | 2878 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| returns when rebaseWhen=never and keepUpdatedLabel does not match  | 2905 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| continues when rebaseWhen=never but checked  | 2932 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| continues when checked by checkedBranches  | 2952 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does nothing when branchPrefixOld/branch and its pr exists  | 2973 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| does nothing when branchPrefixOld/branch and its pr exists but updates not necessary  | 3012 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| Dependency Dashboard All Pending approval  | 3051 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| Dependency Dashboard open all rate-limited  | 3088 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| Dependency Dashboard open all awaiting schedule  | 3125 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| continues branch, skips automerge if there are artifact errors  | 3162 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| continues to update PR, if branch got updated, even when prCreation!==immediate  | 3189 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| checks out baseBranch after committing files  | 3222 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| should not reattempt platform automerge without commitSha  | 3244 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |
| should not reattempt platform automerge in dry run  | 3268 | not-applicable | Mock framework internals — tests update branch index via vitest-mocked platform/SCM/manager; Rust tests this at different layer | — | branch worker runtime behavior is in scope |

---
