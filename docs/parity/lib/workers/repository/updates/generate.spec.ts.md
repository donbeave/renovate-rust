# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/workers/repository/updates/generate.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/workers/repository/updates/generate.spec.ts
**Total tests:** 55 | **Ported:** 0 | **Actionable:** 0 | **Status:** done

### `workers/repository/updates/generate › generateBranchConfig()`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| does not group single upgrade  | 31 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| handles lockFileMaintenance  | 52 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| sets minimumGroupSize based on upgrades  | 77 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | No corresponding Rust source|
| handles lockFileUpdate  | 106 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| does not group same upgrades  | 147 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| groups multiple upgrades same version  | 175 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| groups major updates with different versions but same newValue, no recreateWhen  | 246 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| groups multiple digest updates immortally  | 280 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| recreates grouped pin & pinDigest  | 310 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| does not recreate grouped pin & pinDigest when closed if recreateWhen=never  | 332 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| recreates grouped pin  | 356 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| recreates grouped pinDigest  | 383 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| skips appending baseBranch and updateType to prTitle when prTitleStrict is true  | 409 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| groups multiple upgrades different version  | 462 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| groups multiple upgrades different version but same value  | 513 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| groups multiple upgrades different value but same version  | 553 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| groups multiple digest updates  | 593 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| pins digest to table  | 629 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| fixes different messages  | 647 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| uses semantic commits  | 684 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| calculates the highest priority semanticCommitType  | 711 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| scopes monorepo commits  | 759 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| scopes monorepo commits with nested package files using parent directory  | 786 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| scopes monorepo commits with nested package files using base directory  | 816 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| use prettyVersion in pr title when there is a v  | 845 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| use prettyVersion in pr title there is no v  | 872 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| use newMajor in pr title with v  | 899 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| Default commitMessageExtra pr title  | 924 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| adds commit message body  | 950 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| supports manual prTitle  | 968 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| handles @types specially  | 984 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| handles @types specially (reversed)  | 1049 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| handles upgrades  | 1110 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| combines prBodyColumns  | 1257 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| sorts upgrades, without position first  | 1274 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| passes through pendingChecks  | 1315 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| filters pendingChecks  | 1339 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| displays pending versions  | 1362 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| merge excludeCommitPaths if appears in upgrade  | 1396 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| generates pretty version name properly  | 1429 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| prevents issue with duplicating "v" character  | 1453 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| apply semanticCommits and commitMessagePrefix together  | 1466 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| dedupes duplicate table rows  | 1486 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| using commitMessagePrefix without separator  | 1549 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| merges additionalReviewers  | 1566 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| merges depTypes  | 1590 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| depTypes is available on each branch upgrade object  | 1615 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| allows upgrades in commitMessage  | 1645 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| allows upgrades in commitMessage (group)  | 1670 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| sets skipArtifactsUpdate to false when no upgrades specify a value  | 1725 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| sets skipArtifactsUpdate to true when all upgrades specify true  | 1773 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| sets skipArtifactsUpdate to false when not all upgrades specify true and first is $0  | 1824 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | No corresponding Rust source|
| uses prettyDepType when already set  | 1888 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| falls back to depType when prettyDepType is not set  | 1902 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |
| defaults prettyDepType to dependency when neither prettyDepType nor depType is set  | 1915 | not-applicable | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer | — | Mock framework internals — tests updates generate via vitest-mocked config/platform; Rust tests this at different layer |

---
