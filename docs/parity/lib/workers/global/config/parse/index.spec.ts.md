# Renovate Test Detail

[Back to test map](../../../../../renovate-test-map.md)

## `lib/workers/global/config/parse/index.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/workers/global/config/parse/index.spec.ts
**Total tests:** 35 | **Ported:** 0 | **Actionable:** 0 | **Status:** done-applicable

### `workers/global/config/parse/index › .parseConfigs(env, defaultArgv)`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| supports token in env  | 44 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| supports token in CLI options  | 51 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| supports forceCli  | 69 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| sets customEnvVariables  | 84 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| supports config.force  | 98 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| reads private key from file  | 120 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| supports Bitbucket username/password  | 145 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| massages trailing slash into endpoint  | 163 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| parses global manager config  | 172 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| parses host rules from env  | 179 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| env dryRun = true replaced to full  | 187 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| cli dryRun = true replaced to full  | 197 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| resolves global presets  | 204 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| throws exception if global presets cannot be resolved  | 232 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| cli dryRun replaced to full  | 247 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| env dryRun = false replaced to null  | 254 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| cli dryRun = false replaced to null  | 264 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| only initializes the file when the env var LOG_FILE is properly set  | 271 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| massage onboardingNoDeps when autodiscover is false  | 278 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| does not massage onboardingNoDeps when autodiscover is true  | 289 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| apply secrets to global config  | 299 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| overrides file config with additional file config  | 319 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| merges extends from file config with additional file config  | 334 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| adds extends from fileConfig only  | 352 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| appends files from configFileNames to config filenames list  | 363 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| supports setting configFileNames through cli  | 380 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| supports setting configFileNames through env  | 391 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |

### `workers/global/config/parse/index › .parseConfigs(env, defaultArgv) › when `repositories` is being overridden`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| warns when CLI config overrides repositories from file config  | 405 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| warns when CLI config overrides repositories from env config  | 416 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| does not warn when CLI config sets repositories without override  | 429 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| does not warn when CLI config has no repositories  | 438 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| does not warn when CLI config has same repositories as file config  | 448 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| warns when CLI overrides repositories with repo-specific configuration  | 459 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| does not warn when both values are the same  | 475 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |
| warns when both values are effectively the same  | 487 | not-applicable | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer | — | Mock framework internals — tests global config parse via vitest-mocked fs/config; Rust tests this at different layer |

---
