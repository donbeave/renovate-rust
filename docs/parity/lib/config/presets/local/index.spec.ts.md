# Renovate Test Detail

[Back to test map](../../../../renovate-test-map.md)

## `lib/config/presets/local/index.spec.ts`

**Reference:** https://github.com/renovatebot/renovate/blob/main/lib/config/presets/local/index.spec.ts
**Total tests:** 18 | **Ported:** 0 | **Actionable:** 0 | **Status:** done

### `config/presets/local/index › getPreset()`

| Original test name | Line | Status | Rust file | Rust test name | Reason |
|---|---|---|---|---|---|
| throws for unsupported platform | 34 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to azure | 59 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to bitbucket | 77 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to gerrit | 95 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to custom bitbucket-server | 113 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to gitea | 131 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to forgejo | 149 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to custom gitea | 167 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to custom forgejo | 186 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to github | 205 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to custom github | 223 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to github with a tag | 243 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to custom github with a tag | 262 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to gitlab | 283 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to custom gitlab | 302 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to gitlab with a tag | 322 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| forwards to custom gitlab with a tag | 340 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |
| throws for platform that does not support local presets | 361 | not-applicable | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer | — | Mock framework internals — tests local presets via vitest-mocked fs/platform; Rust tests this at different layer |

---

