# `lib/util/package-rules/jsonata.spec.ts`

[← `util/package-rules`](../../../_by-module/util/package-rules.md) · [all modules](../../../README.md)

**0/0 in-scope tests ported** (0 pending, 12 opt-out) · status: opt-out

| Line | Test | Status | Rust destination / opt-out reason |
|--:|---|---|---|
| 6 | should return true for a matching jsonata expression | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 14 | should return false for a non-matching jsonata expression | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 22 | should return false for an invalid jsonata expression | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 30 | should return null if matchjsonata is not defined | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 35 | should return true for a complex jsonata expression | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 44 | should return false for a complex jsonata expression with non-matching version | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 53 | should return true for a jsonata expression with nested properties | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 62 | should return false for a jsonata expression with nested properties and non-matching version | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 71 | should return true if any jsonata expression matches | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 79 | should catch evaluate errors | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 88 | should return true when sourceurl matches platform | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |
| 96 | should return false when sourceurl does not match platform | opt-out | the jsonata expression evaluation and matching for packageRules (matchJsonata) is not yet implemented (util/jsonata.rs is a stub that always returns null; the tests require a real jsonata engine capable of evaluating expressions against dep objects, handling invalid expressions/errors, supporting complex/nested, and custom functions like $detectPlatform — no direct Rust analogue until the engine is ported) |

