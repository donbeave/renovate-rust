# `lib/util/exec/utils.spec.ts`

[← `util/exec`](../../../_by-module/util/exec.md) · [all modules](../../../README.md)

**3/4 in-scope tests ported** (1 pending, 17 opt-out) · status: partial

| Line | Test | Status | Rust destination / opt-out reason |
|--:|---|---|---|
| 7 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 17 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 27 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 37 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 47 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 57 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 67 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 77 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 88 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 99 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 110 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 120 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 131 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 142 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 153 | is not a commandwithoptions | opt-out | the it() tests with this exact text (in describes for: empty array, command string, mixed strings+booleans, array of booleans, ignoreFailure not boolean, shell not string/boolean, empty shell string, whitespace-only shell, non-empty shell string that makes it not? etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate asserting invalid shape for CommandWithOptions; same reasons as sibling 'is a CommandWithOptions' — TS form detection only, no Rust equiv, core per-item covered. |
| 165 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 176 | is a commandwithoptions | opt-out | the it() tests with this exact text (in various describes: array of 1 command, many commands, valid no ignoreFailure, no shell present, shell=false, shell=true etc.; multiple instances) are pure TS isCommandWithOptions type guard / predicate for valid CommandWithOptions object shape (command: array + optional ignoreFailure: bool, shell: string|bool); no business logic or runtime behavior beyond form detection in the TS wrapper. Rust has no equivalent (uses &[String] + ExecOptions; per-item options like ignoreFailure covered by prior ports in exec/docker/orchestrator/tool_settings); classic TS-runtime type-shape test with no Rust analogue (cf. hostType, AbstractMigration guards, migrations-service reflection, etc. in history and prior opts). |
| 189 | returns array of strings | ported | [`crates/renovate-core/src/util.rs:12247`](../../../../../../crates/renovate-core/src/util.rs#L12247) |
| 198 | returns array of strings | ported | [`crates/renovate-core/src/util.rs:12247`](../../../../../../crates/renovate-core/src/util.rs#L12247) |
| 207 | returns an array of many strings | ported | [`crates/renovate-core/src/util.rs:12266`](../../../../../../crates/renovate-core/src/util.rs#L12266) |
| 220 | returns commands from the `commandwithoptions` | ported | [`crates/renovate-core/src/util.rs:12279`](../../../../../../crates/renovate-core/src/util.rs#L12279) |

