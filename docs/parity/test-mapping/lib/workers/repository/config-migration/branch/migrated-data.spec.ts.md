# `lib/workers/repository/config-migration/branch/migrated-data.spec.ts`

[← `worker/repository`](../../../../../_by-module/worker/repository.md) · [all modules](../../../../../README.md)

**1/1 in-scope tests ported** (0 pending, 18 opt-out) · status: ported

| Line | Test | Status | Rust destination / opt-out reason |
|--:|---|---|---|
| 54 | calls getasync a first when migration not needed | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 62 | calls getasync a first time to initialize the factory | ported | [`crates/renovate-core/src/json_writer.rs:172`](../../../../../../../../crates/renovate-core/src/json_writer.rs#L172) |
| 69 | calls getasync a second time to get the saved data from before | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 77 | gets the filename from the class instance | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 82 | gets the content from the class instance | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 88 | resets the factory and gets a new value | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 95 | resets the factory and gets a new value with default indentation | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 110 | migrate a json5 config file | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 120 | falls back to json.stringify when weave fails | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 138 | uses json.stringify when raw is null | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 150 | returns nothing due to detectrepofileconfig throwing | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 184 | does not format when no prettier config is present | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 193 | does not format when failing to fetch package.json file | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 202 | does not format when there is an invalid package.json file | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 211 | formats when prettier config file is found | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 220 | formats without prettier if in .renovaterc | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 231 | formats when finds prettier config inside the package.json file | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 243 | formats with default 2 spaces | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |
| 259 | formats with printwith=infinity | opt-out | the spec mostly tests the TS MigratedDataFactory class (getAsync first/second, filename/content properties from instance, reset factory, get new value with/without default indent) and the formatting logic (migrate json5, fallback to json.stringify when weave/prettier fails or raw null, does not format when no prettier or fetch package.json fails or invalid package.json, formats when prettier config found or in .renovaterc or inside package.json, formats with default 2 spaces or printwidth=infinity, returns nothing on detectRepoFileConfig throw). The Rust has basic MigratedData struct and json_writer (indent, stringify, strip, 1 ported for factory getAsync init at L172), but the full class/factory async, prettier detection/integration (package.json, .renovaterc, config, json5, printwidth, weave fail, raw null cases) is pending (stubs in apply_prettier_formatting, @parity notes). Core data (filename, content, indent) used in ported create/pr tests. Opt the whole spec as mostly TS class + external prettier behavior with no direct Rust equivalent. |

